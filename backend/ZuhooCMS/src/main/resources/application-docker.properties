# Active inside the backend container (docker-compose sets SPRING_PROFILES_ACTIVE=docker) —
# Postgres is reached via the compose service name, not localhost.
spring.datasource.url=jdbc:postgresql://postgres:5432/businessflow
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234}
